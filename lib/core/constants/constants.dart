import 'package:flutter/foundation.dart';

abstract class Constants {
  static const int snackbarDuration = 3;

  static const topBavVarHeightPadded = kIsWeb ? 180 : 170;
  static const bottomBavVarHeightPadded = kIsWeb ? 90.0 : 60.0;
  static const webMobileSeparationWidth = 600.0;
  static const maxWebWidth = 900.0;

  static const int totalSeeMore = 2;
}
